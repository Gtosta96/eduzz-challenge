import React from 'react';

import styles from './HeroDetails.css';

const HeroDetails = (props) => (
  <div>
    <div>HeroDetails</div>
  </div>
);

HeroDetails.defaultProps = {};
HeroDetails.propTypes = {};

export default HeroDetails;
